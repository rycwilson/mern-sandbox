{
  "name": "node-api",
  "version": "1.0.0",
  "description": "",
  "_comment": ".js files will be treated as .mjs (ES6 module) vs .cjs (CommonJS i.e. Node default module system)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "npx tsc",
    "start": "node build/server.js",
    "dev_old": "concurrently \"npx tsc --watch\" \"nodemon build/server.js --ext html,js,css\"",

    "_comment": "Use `--exec node` else nodemon will attempt to use ts-node (which isn't installed) to compile typescript files",
    "dev": "nodemon --exec node server.ts --ext html,ts,css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ryan Wilson",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^5.1.0",
    "express-rate-limit": "^6.9.0",
    "helmet": "^7.0.0",
    "http-status-codes": "^2.2.0",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.4.5",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.3",
    "@types/cors": "^2.8.14",
    "@types/express": "^4.17.17",
    "@types/jsonwebtoken": "^9.0.2",
    "@types/node": "^20.5.8",
    "concurrently": "^8.2.1",
    "nodemon": "^3.0.1",
    "typescript": "^5.8.3"
  }
}
